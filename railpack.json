{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "install": {
      "commands": [
        "pip install -r web_app/requirements.txt"
      ]
    }
  },
  "deploy": {
    "startCommand": "cd web_app && uvicorn app:app --host 0.0.0.0 --port ${PORT:-8080}"
  }
}
